allprojects  {
    group = 'com.swirlycloud'
    version = '0.1'
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'eclipse'
    apply plugin: 'eclipse-wtp'
    repositories { mavenCentral() }
}
